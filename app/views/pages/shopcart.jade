extends ../layout_index

block content
	.wrap
		div.progress.progress_step_1
			div.progress_step
				span 1
				p SHOPPING CART
			div.progress_step
				span 2
				p YOUR EMAIL
			div.progress_step
				span 3
				p SUCCESS
			div.progress_line
			div.progress_step_line
		table.shopcart_list
			caption
				i.icon-shopping-cart.mr10
				| SHOPPING CART
			tr
				th(width="140px") ITEM
				th.tl DESCRIPTION
				th PRICE
				th QUANTITY
			if product && product.length>0
				each item in product
					tr(id=item._id)
						td
							img(src="/images_data/"+item._id+"/"+item.cover)
						td.shopcart_goods_name
							ul
								li
									h3 #{item.title}
									li MATERIAL: #{item.material}
									li COLOR: #{item.colour}
									li SIZE: 宽#{item.size.w}×深#{item.size.d}×高#{item.size.h}cm
									li
										span.removeBtn.shopcart_goods_remove(data-id="#{item._id}")
											i.icon-remove
											| REMOVE
						td.tc
							b.fs16 $#{item.price}.00
						td.tc #{item.quantity}
				tr
					td(colspan="4")
						a.button.button-royal.button-primary 创建询价单
			else
				tr
					td.tc(colspan="4")
						div.pb20
							p.fb.pt20.pb10 购物车没有任何商品，请前往商品中心添加。
							a(href="/store").button.button-royal.button-primary 商品中心

	.bg_fixed